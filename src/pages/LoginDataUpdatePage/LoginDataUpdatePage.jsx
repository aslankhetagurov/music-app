import AuthForm from '../../components/AuthForm/AuthForm';

const LoginDataUpdatePage = () => {
    return (
        <AuthForm
            title="Update your email or password"
            textBtn="Update"
            type="update"
        />
    );
};

export default LoginDataUpdatePage;
