import AuthForm from '../../components/AuthForm/AuthForm';

const LogInPage = () => {
    return (
        <AuthForm textBtn="Log In" title="Log In in LolMusic" type="logIn" />
    );
};

export default LogInPage;
